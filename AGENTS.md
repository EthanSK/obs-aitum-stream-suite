# Publishing

- After each completed repository change, commit and push it to Ethan's GitHub fork before finishing unless Ethan explicitly says not to publish it.
- Push task branches only to Ethan's fork. Never push directly to the upstream Aitum repository.
- If publishing is blocked, preserve the local change and report the exact blocker instead of silently leaving it unpublished.
